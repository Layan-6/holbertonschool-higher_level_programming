-- Lists all records of second_table ordered by score (top first)
-- Results display score then name
-- Database name is passed as argument to mysql command
SELECT score, name FROM second_table ORDER BY score DESC;
