-- Creates a table called first_table in the current database
-- Table structure: id INT, name VARCHAR(256)
-- If table already exists, script should not fail
CREATE TABLE IF NOT EXISTS first_table (
    id INT,
    name VARCHAR(256)
);
