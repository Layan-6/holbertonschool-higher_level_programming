This directory contains Python projects related to lists and basic data structures.

## Project 0: Print a list of integers

**File:** `0-print_list_integer.py`

**Description:**
A function that prints all integers of a list, one per line.

**Requirements:**
- Use `str.format()` to print integers
- No module imports
- No casting integers to strings

**Example:**
```python
print_list_integer([1, 2, 3, 4, 5])
